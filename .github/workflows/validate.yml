name: Validate

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

'on':
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    uses: ./.github/workflows/reusable-validate.yml
    permissions:
      contents: read
