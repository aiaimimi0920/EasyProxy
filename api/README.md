# API Notes

This directory is reserved for operator-facing API documentation for the
EasyProxy stack.

Current guidance:

- `service/base`
  - management API remains defined by the imported runtime source and deploy
    notes
- `upstreams/misub`
  - machine manifest contract is documented in
    `docs/unified-source-architecture.md`

Follow-up work can promote stable API documentation from module-local sources
into this directory once the monorepo paths settle.
